# Project Instructions

## Supabase Integration

- This project has integrated access to Supabase project `bxpkdevwsvmwuctgixnz`.
- **NEVER** ask the user to run SQL or deploy functions manually.
- Always execute Supabase operations directly using `npx supabase` CLI or the Supabase REST API.
- Project ref: `bxpkdevwsvmwuctgixnz`
- For SQL operations, use the Supabase Management API or `npx supabase db` commands.
- For function deploys, use `npx supabase functions deploy` directly.
- The user should never have to copy/paste SQL or run CLI commands themselves.
